# Notes 1.0.0 — Windows용 마크다운 에디터

## 설치 및 .md 기본앱 연결

1. `Notes-Setup-1.0.0-x64.exe`를 실행합니다. Windows 10/11 64비트용입니다.
2. 설치 후 **설정 → 앱 → 기본 앱**에서 `.md`를 검색하고 **Notes**를 선택합니다. `.markdown`, `.mdown`도 지원합니다.
3. 파일 우클릭 → **연결 프로그램 → Notes**로 열 수도 있습니다. 앱의 **Settings → Files → Default apps** 버튼도 Windows 설정을 엽니다.

기본앱의 최종 선택은 Windows에서 사용자가 한 번 해야 합니다. 앱이 기존 기본앱을 강제로 변경하지 않습니다. 설치 파일에 코드 서명 인증서는 적용되어 있지 않아, Windows에서 게시자를 확인할 수 없다는 안내가 표시될 수 있습니다.

`Notes-Portable-1.0.0-x64.exe`는 설치 없이 실행하는 버전입니다. 기본앱 연결에는 설치형을 사용하세요. 무설치형도 설정과 초안을 Windows 사용자 프로필에 저장합니다.

## 화면과 편집

- 참고 이미지의 영문 Notes 화면, 3단 구성, 문서 탭, SVG 아이콘, 목차, 하단 상태표시줄을 재현했습니다.
- 상단 **Split View**를 누르면 분할/소스 화면, **Preview**를 누르면 미리보기 화면으로 전환합니다. 편집창 사이의 경계선을 끌어 비율을 조절할 수 있습니다.
- Bold, Italic, Heading, List, Checklist, Code, Link, Image, Table, Quote가 실제 Markdown을 삽입합니다.
- 미리보기 체크박스는 원문과 연결됩니다. 코드 블록의 복사 아이콘도 작동합니다.
- 초안은 자동으로 보관됩니다. 실제 `.md` 파일에 저장하려면 **Ctrl+S**, 다른 이름으로 저장하려면 **Ctrl+Shift+S**를 사용합니다.
- 이미 열어 둔 파일이 외부에서 변경됐다면 덮어쓰기를 차단합니다. **다른 이름으로 저장**하면 두 버전을 보존할 수 있습니다.
- 탭이나 최근 파일을 우클릭하면 파일의 작업공간 이름, 태그, 즐겨찾기, 분류를 수정할 수 있습니다. 기존 디스크 파일의 이름 변경은 **다른 이름으로 저장**을 사용합니다.
- Trash에 넣은 노트는 복원할 수 있습니다. 삭제를 동기화할 때도 휴지통 상태로 전달합니다.
- **Export**에서 Markdown, HTML, PDF로 저장합니다. 로컬 이미지 링크를 포함한 HTML/Markdown을 다른 PC로 이동할 때는 이미지도 함께 옮겨야 합니다.

샘플 문서에 있는 `In Progress`, `Planned` 문구와 체크리스트는 이미지에 맞춘 예제 본문입니다. 실제 앱의 기능 구현 상태를 나타내는 표시가 아닙니다. 상태표시줄의 단어 수와 동기화 상태는 실제 데이터로 계산합니다.

## 폰트·테마·투명도

- **Settings → Appearance**: Light/Dark/System 테마, 창 투명도 70~100%, 패널 투명도, 미리보기 폰트와 크기.
- **Settings → Editor**: 설치된 폰트 선택, 편집 폰트 크기, 줄바꿈, 스크롤 연동, 사용자 폰트 추가.
- **Import font**는 TTF/OTF/WOFF/WOFF2를 지원합니다. 한글 글꼴도 사용할 수 있습니다.
- 상단 달/해 SVG 아이콘으로 테마를 바로 전환합니다.
- Windows 11 22H2 이상에서는 OS 배경 효과를 추가로 사용합니다. Windows 10에서는 창 투명도 조절이 적용됩니다.

화면의 배치와 주요 간격을 비교해 구현했지만, Windows 버전·배율·폰트 렌더링 차이를 포함한 픽셀 단위 완전 일치는 보증하지 않습니다.

## OneDrive·Dropbox·Google Drive

1. 사용하는 클라우드의 일반 PC 동기화 앱에 로그인합니다.
2. **Settings → Sync**에서 서비스를 선택합니다.
3. 해당 클라우드가 동기화하는 폴더를 선택합니다.
4. **Sync now**를 누릅니다. 다른 PC에서도 같은 클라우드 폴더를 선택합니다.

개발자 계정이나 API 키는 필요하지 않습니다. 앱이 각 클라우드 API에 직접 로그인하는 방식이 아니라, 이미 로그인된 PC 동기화 폴더를 이용합니다.

`Notes Workspace/workspace.json`이 동기화 원본이며 `Markdown` 하위 폴더에는 `.md` 사본을 출력합니다. 노트 편집은 Notes에서 하세요. 출력된 `.md` 사본을 다른 편집기로 수정한 내용은 자동으로 가져오지 않습니다.

## GitHub

1. **Settings → Sync → GitHub**를 선택합니다.
2. **Sign in with browser**를 누르고 표시되는 일회용 코드를 브라우저에 입력합니다. 이 PC에서 GitHub CLI에 로그인했다면 기존 로그인을 인식합니다.
3. 쓰기 권한이 있는 기존 저장소를 `계정/저장소` 형식으로 입력하거나 목록에서 선택합니다.
4. **Connect → Sync now**를 누릅니다.

GitHub CLI가 앱에 포함되어 있어 별도 설치, Git 설치, 개인용 토큰이나 개발자 키 발급이 필요하지 않습니다. 개인 문서는 비공개 저장소를 선택하세요. 앱은 해당 저장소의 `.notes-sync/workspace.json`만 갱신합니다. 기본 브랜치에 직접 쓰기가 허용되어 있어야 합니다.

동기화는 수동 실행 및 앱 실행 중 5분 주기로 작동합니다. 양쪽에서 같은 노트를 수정하면 충돌 사본을 남깁니다. GitHub가 동기화 도중 다른 PC의 변경을 감지하면 재동기화를 안내합니다. 오프라인 PC들이 동시에 클라우드 폴더를 갱신하여 클라우드 제공자가 별도 충돌 파일을 만드는 경우, 복구를 위해 그 파일을 보관하세요.

## 검증 범위

실제 Windows 11 호스트에서 실행형을 점검했습니다. 한글 파일 입출력과 경로, 시작 시 파일 열기, 테마, 실제 창 투명도, 폰트 선택/추가, Markdown 미리보기, 체크박스, PDF 출력, 검색, 초안 복원, 종료 직전 저장을 확인했습니다. 1550/1280/980 픽셀 창 너비에서 도구막대 넘침도 확인했습니다.

독립 저장소 테스트에서 두 기기의 변경 전송/수신, 충돌 사본 생성, 외부 파일 덮어쓰기 방지, GitHub 전송과 동시 갱신 거부를 검증했습니다. 세 클라우드 서비스의 폴더 어댑터는 로컬 테스트로 검증했습니다. 사용자 계정의 실제 클라우드 네트워크 왕복, GitHub의 새 브라우저 로그인 승인, 별도 Windows 10 가상머신, 설치 후 Windows 기본앱 설정 변경까지 완료했다고 주장하지 않습니다.

소스: https://github.com/9to6blog/line-md-editor
